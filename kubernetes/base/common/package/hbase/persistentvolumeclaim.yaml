kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: package-hbase-client
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: zookage-package-hbase-client
