apiVersion: v1
kind: PersistentVolume
metadata:
  name: zookage-package-ozone
spec:
  accessModes:
  - ReadOnlyMany
  capacity:
    storage: 1Gi
  hostPath:
    path: /opt/zookage/ozone
  storageClassName: zookage-package-ozone
