kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: package-ozone
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: zookage-package-ozone
