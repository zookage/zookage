kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: package-spark
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: zookage-package-spark
