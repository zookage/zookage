apiVersion: v1
kind: PersistentVolume
metadata:
  name: zookage-package-trino
spec:
  accessModes:
  - ReadOnlyMany
  capacity:
    storage: 1Gi
  hostPath:
    path: /opt/zookage/trino
  storageClassName: zookage-package-trino
