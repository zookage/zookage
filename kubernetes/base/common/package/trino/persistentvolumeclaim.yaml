kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: package-trino
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: zookage-package-trino
