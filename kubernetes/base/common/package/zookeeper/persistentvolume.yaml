apiVersion: v1
kind: PersistentVolume
metadata:
  name: zookage-package-zookeeper
spec:
  accessModes:
  - ReadOnlyMany
  capacity:
    storage: 1Gi
  hostPath:
    path: /opt/zookage/zookeeper
  storageClassName: zookage-package-zookeeper
