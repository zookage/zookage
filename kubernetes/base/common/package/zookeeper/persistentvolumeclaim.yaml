kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: package-zookeeper
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: zookage-package-zookeeper
